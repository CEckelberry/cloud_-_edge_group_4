sed \
    -e 's/{{TOPIC}}/group4/g' \
    -e "s/{{GROUP_ID}}/"$(cat /proc/sys/kernel/random/uuid | tr -d "-")"/g" < ./consumer_template.yml \
    | kubectl apply -f -


docker build -t consumer .

docker run -i consumer consumer.py "group4"

assignment/start-producer.sh group4 